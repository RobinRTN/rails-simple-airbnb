<p style="background-image: url('app/assets/images/Screenshot 2022-11-16 at 16.30.10.png');">



</p>

<h1 class="mt-2 ms-5">All our flats (<%=@flats.count%>)</h1>
<div class="container d-flex">
  <div class="row">
    <%@flats.each do |flat|%>
      <div class="card mt-2 ms-4 col-4 shadow-sm p-3 mb-5 bg-white rounded" style="width: 18rem;">
        <div class="card-body">
          <h5 class="card-title"><%=flat.name%></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%=flat.address%></h6>
          <p><em><%=flat.description%></em></p>
          <div class="icons d-flex d-flex align-items-center">
            <p class="me-2">💲<%=flat.price_per_night%></p>
            <p>🤠<%=flat.number_of_guests%></p>
          </div>
        </div>
        <div class="links d-flex align-items-center">
          <%= link_to "See more", flat_path(flat), class: "me-2 text-primary text-decoration-none"%>
          <%= link_to "Edit", edit_flat_path(flat),class: "me-2 text-warning text-decoration-none" %></p>
          <%= link_to "Remove", flat_path(flat), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: "me-2 text-danger text-decoration-none"%>
        </div>

      </div>
    <%end%>
  </div>
</div>


